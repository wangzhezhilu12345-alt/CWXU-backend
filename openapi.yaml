# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Auth API
    version: 0.0.1
paths:
    /api/v1/auth/admin/change-password:
        post:
            tags:
                - Auth
            operationId: Auth_AdminChangePassword
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/AdminChangePasswordReq'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AdminChangePasswordResp'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/auth/admin/login:
        post:
            tags:
                - Auth
            operationId: Auth_AdminLogin
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/AdminLoginReq'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/AdminLoginResp'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/auth/student/info:
        get:
            tags:
                - Auth
            operationId: Auth_StudentInfo
            parameters:
                - name: stuNo
                  in: query
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/StudentInfoResp'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /api/v1/auth/student/login:
        post:
            tags:
                - Auth
            operationId: Auth_StudentLogin
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/StudentLoginReq'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/StudentLoginResp'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        AdminChangePasswordReq:
            type: object
            properties:
                username:
                    type: string
                oldPassword:
                    type: string
                newPassword:
                    type: string
        AdminChangePasswordResp:
            type: object
            properties:
                message:
                    type: string
        AdminLoginReq:
            type: object
            properties:
                username:
                    type: string
                password:
                    type: string
        AdminLoginResp:
            type: object
            properties:
                message:
                    type: string
                token:
                    type: string
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        StudentData:
            type: object
            properties:
                studentNo:
                    type: string
                name:
                    type: string
                token:
                    type: string
        StudentDetailInfo:
            type: object
            properties:
                id:
                    type: integer
                    format: uint32
                name:
                    type: string
                sex:
                    type: string
                studentNo:
                    type: string
                idCardNo:
                    type: string
        StudentInfoResp:
            type: object
            properties:
                message:
                    type: string
                data:
                    $ref: '#/components/schemas/StudentDetailInfo'
        StudentLoginReq:
            type: object
            properties:
                stuNo:
                    type: string
                cardNo:
                    type: string
                taskId:
                    type: integer
                    format: uint32
        StudentLoginResp:
            type: object
            properties:
                message:
                    type: string
                data:
                    $ref: '#/components/schemas/StudentData'
tags:
    - name: Auth
